#include <stdio.h>
#include <math.h>


int main()
{
    float f,t;
    if(t>=0)
    {
         printf("f(t) is equal to %.2f",f);

    }
    else(f = -3 * (pow(t,2)) - 5);
    {
        printf("f(t) is equal to %.2f",f);
    }
}