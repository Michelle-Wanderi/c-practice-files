#include <stdio.h>
#include <math.h>
#define P 1000000

int main()
{
    int rate  ,time;
    float n = rate / 100;
    float amount;
    
   
   if(rate= 6, time= 60)
   {
    amount = 
          P * (pow(1 + n, time));
     printf("Your total amount is,%f",amount);
   } 

    else if(rate = 12 , time= 30);
    {
     amount = 
          P * (pow(1 + n, time));
    printf("Your total amount is,%f",amount);

    }
}